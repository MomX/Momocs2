% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/morpho_calibrate.R
\name{opoly_calibrate_reconstruction}
\alias{opoly_calibrate_reconstruction}
\title{Calibrate polynomial degree using visual reconstruction}
\usage{
opoly_calibrate_reconstruction(
  x,
  degree_range = 2:10,
  id = NULL,
  nb_pts = 120,
  .cols = NULL
)
}
\arguments{
\item{x}{A tibble with coo columns containing open curves.}

\item{degree_range}{Integer vector. Range of degrees to display. Default is 2:10 (9 panels).}

\item{id}{Integer or NULL. Which shape to display. If NULL, randomly samples one.}

\item{nb_pts}{Integer. Number of points for reconstruction. Default is 120.}

\item{.cols}{Column name(s) to process. If NULL, uses first coo column.
Supports tidyeval (e.g., \code{.cols = VD}).}
}
\value{
A list with class \code{c("calibrate_reconstruction", "calibrate")} containing:
\itemize{
\item \code{data}: List of reconstructed coordinates at each degree
\item \code{original}: Original shape coordinates (not scaled)
\item \code{degree_range}: The degrees displayed
\item \code{id}: Index of shape used
}

The list has attributes:
\itemize{
\item \code{method}: "opoly"
\item \code{metric}: "reconstruction"
}
}
\description{
Visually compare reconstructed shapes at different polynomial degrees to
assess fit quality and determine optimal degree.
}
\details{
This function creates a multi-panel plot (3x3 grid for default 9 degrees) showing:
\itemize{
\item Original shape in black
\item Reconstructed shape in red
\item One panel per degree in \code{degree_range}
\item Visual assessment of how reconstruction improves with degree
}

This is the most intuitive calibration method - you can directly see when
adding more degrees stops meaningfully improving the fit.
}
\examples{
# Calibrate on single shape from olea (creates 3x3 grid)
data(olea)
olea[1:10, ] \%>\% opoly_calibrate_reconstruction(id = 1, .cols=VD)

# Try different degree ranges
olea[1:10,] \%>\% opoly_calibrate_reconstruction(id = 5, degree_range = 3:11, .cols=VD)

# Tidyeval column selection
olea[1:10, ] \%>\% opoly_calibrate_reconstruction(.cols = VL, id = 1)

# Random shape
olea \%>\% opoly_calibrate_reconstruction(.cols=VD)  # samples random shape

}
\seealso{
\code{\link[=opoly_calibrate_r2]{opoly_calibrate_r2()}}, \code{\link[=opoly]{opoly()}}
}
