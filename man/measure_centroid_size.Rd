% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/measure.R
\name{measure_centroid_size}
\alias{measure_centroid_size}
\title{Measure centroid size}
\usage{
measure_centroid_size(x, ..., .cols = NULL, .name = NULL)
}
\arguments{
\item{x}{A matrix (nx2), list of matrices, or tibble with coo columns.}

\item{...}{Additional arguments (reserved for future use).}

\item{.cols}{Column name(s) to process when \code{x} is a tibble. If \code{NULL},
automatically detects columns containing coo objects.}

\item{.name}{Character. Name for the new column when \code{x} is a tibble. If \code{NULL},
uses "colname_centroid_size" (e.g., "coo_centroid_size").}
}
\value{
\itemize{
\item If \code{x} is a single matrix: returns a numeric scalar
\item If \code{x} is a list: returns a numeric vector of class "meas"
\item If \code{x} is a tibble: returns the tibble with new centroid size column(s) added
}
}
\description{
Calculate the centroid size: the square root of the sum of squared
distances from each point to the centroid.
}
\details{
Centroid size is a common size measure in geometric morphometrics.
It is scale-independent and used for allometric correction.
}
\examples{
measure_centroid_size(shapes$cat)
measure_centroid_size(shapes)
measure_centroid_size(bot)
measure_centroid_size(bot, .name = "CS")

}
\seealso{
\code{\link[=measure_centroid_size_norm]{measure_centroid_size_norm()}} for normalized centroid size; \code{\link[=get_centroid_size]{get_centroid_size()}} for the getter function
}
\keyword{internal}
