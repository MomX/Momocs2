% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get.R
\name{get_compactness}
\alias{get_compactness}
\title{Get compactness}
\usage{
get_compactness(x, ..., .cols = NULL)
}
\arguments{
\item{x}{A matrix (nx2), list of matrices, or tibble with coo columns.}

\item{...}{Additional arguments (reserved for future use).}

\item{.cols}{Column name(s) to process when \code{x} is a tibble. If \code{NULL},
automatically detects columns containing coo objects.}
}
\value{
\itemize{
\item If \code{x} is a single matrix: returns a numeric scalar
\item If \code{x} is a list: returns a numeric vector
\item If \code{x} is a tibble: returns a numeric vector extracted from coo column
}
}
\description{
Calculate shape compactness (Miller's index).
}
\details{
Compactness (Miller's index) = Area / Area(bounding_box).
Value of 1 for shapes that completely fill their bounding box (rectangles).
Lower values indicate less compact shapes.

Note: Polsby-Popper compactness (4π × Area / Perimeter²) is available
as \code{get_circularity()}.
}
\examples{
get_compactness(shapes$cat)

# Use in measure()
bot \%>\% measure("compactness")

}
\seealso{
\code{\link[=get_circularity]{get_circularity()}}, \code{\link[=get_rectangularity]{get_rectangularity()}}
}
\keyword{internal}
