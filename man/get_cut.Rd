% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get.R
\name{get_cut}
\alias{get_cut}
\title{Cut outline at landmarks}
\usage{
get_cut(x, ldk = NULL, ..., .cols = NULL, .ldk_col = NULL)
}
\arguments{
\item{x}{A matrix (nx2), list of matrices, or tibble with coo columns.}

\item{ldk}{Integer vector. Landmark indices to cut at. Required for single matrix.}

\item{...}{Additional arguments (reserved for future use).}

\item{.cols}{Column name(s) to process when \code{x} is a tibble. If \code{NULL},
automatically detects columns containing coo columns.}

\item{.ldk_col}{Character. Name of landmark column. If \code{NULL}, uses \code{colname_ldk}.}
}
\value{
\itemize{
\item If \code{x} is a single matrix: returns a list of matrices (open curves with class 'cur')
\item If \code{x} is a list: returns a list of lists (one per shape)
\item If \code{x} is a tibble: returns a tibble with _cur1, _cur2, etc. columns
}
}
\description{
Split an outline into open curves at landmark positions.
}
\details{
Splits an outline into segments between consecutive landmarks. Each segment
is an open curve that starts and ends at landmarks.

For an outline with landmarks at positions 3 and 7 (8 points total):
\itemize{
\item Curve 1: points 3→4→5→6→7
\item Curve 2: points 7→8→1→2→3 (wraps around)
}

Requires at least 2 landmarks. Landmarks are included at the start and end
of each curve.
}
\examples{
# Cut outline at landmarks
coo <- matrix(1:20, ncol=2)
get_cut(coo, ldk = c(3, 7))

}
\seealso{
\code{\link[=get_join]{get_join()}}
}
\keyword{internal}
