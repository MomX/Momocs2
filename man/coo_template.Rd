% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coo.R
\name{coo_template}
\alias{coo_template}
\alias{coo_template_relatively}
\title{Template shapes to standard size}
\usage{
coo_template(x, ..., .cols = NULL, .ldk_col = NULL)

coo_template_relatively(x, ..., .cols = NULL, .ldk_col = NULL)
}
\arguments{
\item{x}{A matrix (nx2), list of matrices, or tibble with coo columns.}

\item{...}{Additional arguments (reserved for future use).}

\item{.cols}{Column name(s) to process when \code{x} is a tibble. If \code{NULL},
automatically detects columns containing coo objects.}

\item{.ldk_col}{Character. Name of landmark column. If \code{NULL}, uses \code{colname_ldk}.}

\item{size}{Numeric. Target size for the largest dimension. Default is 1.}
}
\value{
\itemize{
\item If \code{x} is a single matrix: returns the templated matrix
\item If \code{x} is a list: returns a list of templated matrices
\item If \code{x} is a tibble: returns the tibble with templated coo column(s)
}
}
\description{
Scale and center shapes to fit in a standard template box.
}
\details{
\strong{\code{coo_template()}}: Scales each shape independently so its largest dimension
equals \code{size}, then centers it at the origin (0, 0).

\strong{\code{coo_template_relatively()}}: Scales shapes while preserving relative sizes.
The largest shape gets \code{size}, others are scaled proportionally smaller.
All shapes are centered at (0, 0).

For a single matrix, both functions produce identical results.

\code{coo_template_relatively()} is perfect for \code{mosaic()} to create family pictures
where size relationships are preserved!
}
\examples{
# Single shape - both functions are identical
coo_template(shapes$cat, size = 1)
coo_template_relatively(shapes$cat, size = 1)

# Multiple shapes - independent scaling
shapes \%>\% coo_template(size = 1)

# Multiple shapes - relative scaling
shapes \%>\% coo_template_relatively(size = 1)

# Perfect for mosaic - shows true size relationships
shapes \%>\%
  coo_template_relatively(size = 1) \%>\%
  mosaic()

}
\seealso{
\code{\link[=mosaic]{mosaic()}}, \code{\link[=coo_center]{coo_center()}}
}
\keyword{internal}
