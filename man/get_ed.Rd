% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get.R
\name{get_ed}
\alias{get_ed}
\alias{get_ed_along}
\alias{get_ed_intermediate}
\title{Euclidean distance measurements}
\usage{
get_ed(x, y)

get_ed_along(x, y)

get_ed_intermediate(x, y, r)
}
\arguments{
\item{x}{A matrix (nx2) or numeric vector of length 2.}

\item{y}{A matrix (nx2) or numeric vector of length 2.}

\item{r}{Numeric. Interpolation parameter between 0 and 1.}
}
\value{
\itemize{
\item \code{get_ed()}: numeric scalar
\item \code{get_ed_along()}: numeric vector
\item \code{get_ed_intermediate()}: matrix (same dimensions as inputs)
}
}
\description{
Calculate Euclidean distances between points or shapes.
}
\details{
\itemize{
\item \code{get_ed()}: distance between two points or total distance between shapes
\item \code{get_ed_along()}: point-wise distances between two conformable matrices
\item \code{get_ed_intermediate()}: interpolated point(s) along line between x and y
}

These functions do not use the \code{make_get_function} dispatcher as they require
two arguments (x and y).
}
\examples{
p1 <- c(0, 0)
p2 <- c(3, 4)
get_ed(p1, p2)

get_ed_along(shapes$cat, shapes$dog)
get_ed_intermediate(p1, p2, r = 0.5)

}
\keyword{internal}
