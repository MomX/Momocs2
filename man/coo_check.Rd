% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coo.R
\name{coo_check}
\alias{coo_check}
\title{Check and clean coordinates}
\usage{
coo_check(x, ..., .cols = NULL, .ldk_col = NULL)
}
\arguments{
\item{x}{A matrix (nx2), list of matrices, or tibble with coo columns.}

\item{...}{Additional arguments (reserved for future use).}

\item{.cols}{Column name(s) to process when \code{x} is a tibble. If \code{NULL},
automatically detects columns containing coo objects.}

\item{.ldk_col}{Character. Name of landmark column. If \code{NULL}, uses \code{colname_ldk}.}

\item{remove_na}{Logical. Remove rows with NA values. Default TRUE.}

\item{remove_duplicates}{Logical. Remove consecutive duplicate points. Default TRUE.}

\item{remove_infinite}{Logical. Remove rows with Inf/-Inf values. Default TRUE.}

\item{min_points}{Integer. Warn if fewer than this many points remain. Default 3.}

\item{warn_collinear}{Logical. Warn if all points are collinear. Default TRUE.}
}
\value{
\itemize{
\item If \code{x} is a single matrix: returns the cleaned matrix
\item If \code{x} is a list: returns a list of cleaned matrices
\item If \code{x} is a tibble: returns the tibble with cleaned coo column(s)
}
}
\description{
Remove problematic rows (NA values, consecutive duplicates, infinite values)
and warn about potential issues.
}
\details{
Performs the following checks and cleaning operations:
\enumerate{
\item \strong{Remove NA rows}: Rows containing NA in either coordinate
\item \strong{Remove consecutive duplicates}: Identical adjacent points
\item \strong{Remove infinite values}: Rows containing Inf or -Inf
\item \strong{Warn if too few points}: Less than \code{min_points} remain
\item \strong{Warn if collinear}: All points lie on a line
}

Messages indicate which shapes were affected and what was removed.
Landmarks are synced - if a point is removed, its landmark is also removed.
}
\examples{
# Clean a single shape
coo_check(shapes$cat)

# Clean all shapes in list
coo_check(bot$coo)

# Clean all shapes in tibble
coo_check(bot)

# Disable specific checks
coo_check(bot, remove_duplicates = FALSE, warn_collinear = FALSE)

}
\seealso{
\code{\link[=coo_sample]{coo_sample()}} for resampling
}
\keyword{internal}
