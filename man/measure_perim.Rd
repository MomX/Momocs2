% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/measure.R
\name{measure_perim}
\alias{measure_perim}
\title{Measure perimeter of an outline}
\usage{
measure_perim(x, ..., .cols = NULL, .name = NULL)
}
\arguments{
\item{x}{A matrix (nx2), list of matrices, or tibble with coo columns.}

\item{...}{Additional arguments (reserved for future use).}

\item{.cols}{Column name(s) to process when \code{x} is a tibble. If \code{NULL},
automatically detects columns containing coo objects.}

\item{.name}{Character. Name for the new column when \code{x} is a tibble. If \code{NULL},
uses "colname_perim" (e.g., "coo_perim").}
}
\value{
\itemize{
\item If \code{x} is a single matrix: returns a numeric scalar
\item If \code{x} is a list: returns a numeric vector of class "meas"
\item If \code{x} is a tibble: returns the tibble with new perimeter column(s) added
}
}
\description{
Calculate the total perimeter (length) of a shape.
}
\details{
Computes the sum of Euclidean distances between consecutive points,
including the distance from the last point back to the first.
}
\examples{
measure_perim(shapes$cat)
measure_perim(shapes)
measure_perim(bot)
measure_perim(bot, .name = "perimeter")

}
\seealso{
\code{\link[=measure_area]{measure_area()}} for area; \code{\link[=get_perim]{get_perim()}} for the getter function
}
\keyword{internal}
