% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get.R
\name{get_range}
\alias{get_range}
\title{Get coordinate ranges}
\usage{
get_range(x, ..., .cols = NULL)
}
\arguments{
\item{x}{A matrix (nx2), list of matrices, or tibble with coo columns.}

\item{...}{Additional arguments (reserved for future use).}

\item{.cols}{Column name(s) to process when \code{x} is a tibble. If \code{NULL},
automatically detects columns containing coo objects.}
}
\value{
\itemize{
\item If \code{x} is a single matrix: returns a 2x2 matrix (rows: min/max, cols: x/y)
\item If \code{x} is a list: returns a list of 2x2 matrices
\item If \code{x} is a tibble: returns a list of 2x2 matrices extracted from coo column
}
}
\description{
Calculate the range (min, max) for each coordinate axis.
}
\details{
Returns a matrix where row 1 is min values and row 2 is max values for x and y.
This is not a scalar, so cannot be used with \code{measure()}.
}
\examples{
get_range(shapes$cat)
get_range(shapes)

}
\seealso{
\code{\link[=get_range_diff]{get_range_diff()}}
}
\keyword{internal}
