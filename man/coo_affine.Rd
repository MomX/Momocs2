% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coo.R
\name{coo_affine}
\alias{coo_affine}
\title{Apply affine transformation}
\usage{
coo_affine(x, ..., .cols = NULL, .ldk_col = NULL)
}
\arguments{
\item{x}{A matrix (nx2), list of matrices, or tibble with coo columns.}

\item{...}{Additional arguments (reserved for future use).}

\item{.cols}{Column name(s) to process when \code{x} is a tibble. If \code{NULL},
automatically detects columns containing coo objects.}

\item{mat}{A 2x2 numeric matrix defining the affine transformation.}
}
\value{
\itemize{
\item If \code{x} is a single matrix: returns the transformed matrix
\item If \code{x} is a list: returns a list of transformed matrices
\item If \code{x} is a tibble: returns the tibble with specified coo columns transformed
}
}
\description{
Apply an affine transformation (rotation, scaling, shearing, etc.) to a shape
using a 2x2 transformation matrix.
}
\details{
The transformation is applied as: \code{x_new = x \%*\% mat}

Common transformation matrices:
\itemize{
\item Rotation by angle θ: \code{matrix(c(cos(θ), sin(θ), -sin(θ), cos(θ)), nrow = 2)}
\item Scaling by sx, sy: \code{matrix(c(sx, 0, 0, sy), nrow = 2)}
\item Shear in x: \code{matrix(c(1, 0, k, 1), nrow = 2)}
}
}
\examples{
# Rotation matrix
rot_mat <- matrix(c(cos(pi/4), sin(pi/4), -sin(pi/4), cos(pi/4)), nrow = 2)
coo_affine(shapes$cat, rot_mat)

# Scaling matrix
scale_mat <- matrix(c(2, 0, 0, 2), nrow = 2)
coo_affine(shapes, scale_mat)

}
\seealso{
\code{\link[=coo_rotate]{coo_rotate()}} for rotation; \code{\link[=coo_shear]{coo_shear()}} for shearing; \code{\link[=coo_flip]{coo_flip()}} for flipping
}
\keyword{internal}
