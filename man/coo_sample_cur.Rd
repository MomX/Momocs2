% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coo.R
\name{coo_sample_cur}
\alias{coo_sample_cur}
\alias{coo_sample_cur_prop}
\title{Sample coordinates for open curves}
\usage{
coo_sample_cur(x, ..., .cols = NULL, .ldk_col = NULL)

coo_sample_cur_prop(x, ..., .cols = NULL, .ldk_col = NULL)
}
\arguments{
\item{x}{A matrix (nx2), list of matrices, or tibble with coo columns.}

\item{...}{Additional arguments passed to internal functions:
\itemize{
\item \code{n}: Integer. Target number of points (minimum 3)
\item \code{prop}: Numeric. Proportion of points to keep (0 to 1)
}}

\item{.cols}{Column name(s) to process when \code{x} is a tibble. If \code{NULL},
automatically detects columns containing coo objects.}

\item{.ldk_col}{Character. Name of landmark column. If \code{NULL}, uses \code{colname_ldk}.}
}
\value{
\itemize{
\item If \code{x} is a single matrix: returns the resampled matrix
\item If \code{x} is a list: returns a list of resampled matrices
\item If \code{x} is a tibble: returns the tibble with specified coo columns resampled
}
}
\description{
Resample an open curve to a different number of points, preserving first and last points.
}
\details{
\itemize{
\item \code{coo_sample_cur()}: resample to exactly n points via arc-length interpolation
\item \code{coo_sample_cur_prop()}: resample to a proportion of original points
}

\strong{Key differences from \code{coo_sample()}:}
\itemize{
\item Designed for open curves (not closed outlines)
\item Always preserves EXACT first and last point coordinates
\item Does NOT wrap around (no connection from last to first point)
\item Samples linearly along the curve from start to end
}

These functions are landmark-aware: if landmarks are present, the curve
is resampled segment-by-segment between landmarks, with first and last
points treated as implicit landmarks. Points are distributed proportionally
across segments based on arc length.
}
\examples{
# Resample open curve to 100 points
data(olea)
curve <- olea$VD[[1]]
coo_sample_cur(curve, n = 100)

# First and last points are preserved exactly
original <- olea$VD[[1]]
resampled <- coo_sample_cur(original, n = 50)
all.equal(original[1, ], resampled[1, ])      # TRUE
all.equal(original[99, ], resampled[50, ])    # TRUE

# Resample list
coo_sample_cur(olea$VD[1:5], n = 80)

# Resample to 50\% of points
coo_sample_cur_prop(curve, prop = 0.5)

# Tibble workflow
olea \%>\% coo_sample_cur(.cols=VD, n = 60)

}
\seealso{
\code{\link[=coo_sample]{coo_sample()}} for closed outlines; \code{\link[=get_coords_nb]{get_coords_nb()}} for point count;
\code{\link[=coo_smooth]{coo_smooth()}} for smoothing
}
