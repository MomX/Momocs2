% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get.R
\name{get_centroid_distance}
\alias{get_centroid_distance}
\title{Get distances from centroid}
\usage{
get_centroid_distance(x, ..., .cols = NULL)
}
\arguments{
\item{x}{A matrix (nx2), list of matrices, or tibble with coo columns.}

\item{...}{Additional arguments (reserved for future use).}

\item{.cols}{Column name(s) to process when \code{x} is a tibble. If \code{NULL},
automatically detects columns containing coo objects.}
}
\value{
\itemize{
\item If \code{x} is a single matrix: returns a numeric vector (one distance per point)
\item If \code{x} is a list: returns a list of numeric vectors
\item If \code{x} is a tibble: returns a list of numeric vectors extracted from coo column
}
}
\description{
Calculate the distance from the centroid to each point.
}
\details{
Returns the Euclidean distance from the centroid to each point on the outline.
This is a vector, not a scalar, so cannot be used with \code{measure()}.
}
\examples{
get_centroid_distance(shapes$cat)
get_centroid_distance(shapes)

}
\seealso{
\code{\link[=get_centroid]{get_centroid()}}, \code{\link[=get_centroid_size]{get_centroid_size()}}
}
\keyword{internal}
