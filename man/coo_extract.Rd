% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coo.R
\name{coo_extract}
\alias{coo_extract}
\alias{coo_head}
\alias{coo_tail}
\title{Extract or subset coordinates}
\usage{
coo_extract(x, ..., .cols = NULL, .ldk_col = NULL)

coo_head(x, ..., .cols = NULL, .ldk_col = NULL)

coo_tail(x, ..., .cols = NULL, .ldk_col = NULL)
}
\arguments{
\item{x}{A matrix (nx2), list of matrices, or tibble with coo columns.}

\item{...}{Additional arguments (reserved for future use).}

\item{.cols}{Column name(s) to process when \code{x} is a tibble. If \code{NULL},
automatically detects columns containing coo objects.}

\item{.ldk_col}{Character. Name of landmark column. If \code{NULL}, uses \code{colname_ldk}.}

\item{id}{Integer vector of row indices to extract (for \code{coo_extract()}).}

\item{n}{Integer. Number of points to keep or remove.}
}
\value{
\itemize{
\item If \code{x} is a single matrix: returns a matrix with selected/trimmed rows
\item If \code{x} is a list: returns a list of matrices
\item If \code{x} is a tibble: returns the tibble with specified coo columns subsetted
}
}
\description{
Subset or trim shapes by row index or position.
}
\details{
\itemize{
\item \code{coo_extract()}: extract specific rows by index
\item \code{coo_head()}: keep first n points
\item \code{coo_tail()}: keep all but the last n points
}

If \code{n} equals \code{nrow(x)}, the shape is returned unchanged.

These functions are landmark-aware: invalid landmark indices are automatically
filtered out after subsetting.
}
\examples{
coo_extract(shapes$cat, c(1, 10, 50))
coo_extract(shapes, c(1, 6))
coo_head(shapes$cat, 10)
coo_head(shapes, 3)
coo_tail(shapes$cat, 5)
coo_head(bot, 20)

}
\seealso{
\code{\link[=coo_sample]{coo_sample()}} for resampling to different point counts
}
\keyword{internal}
