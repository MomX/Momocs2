% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get.R
\name{get_bbox}
\alias{get_bbox}
\title{Get bounding box corners}
\usage{
get_bbox(x, ..., .cols = NULL)
}
\arguments{
\item{x}{A matrix (nx2), list of matrices, or tibble with coo columns.}

\item{...}{Additional arguments (reserved for future use).}

\item{.cols}{Column name(s) to process when \code{x} is a tibble. If \code{NULL},
automatically detects columns containing coo objects.}
}
\value{
\itemize{
\item If \code{x} is a single matrix: returns a 4x2 matrix with corners
\item If \code{x} is a list: returns a list of 4x2 matrices
\item If \code{x} is a tibble: returns a list of 4x2 matrices extracted from coo column
}
}
\description{
Calculate the four corners of the bounding box.
}
\details{
Returns the four corners of the axis-aligned bounding box as a 4x2 matrix:
\itemize{
\item Row 1: bottom-left (xmin, ymin)
\item Row 2: bottom-right (xmax, ymin)
\item Row 3: top-right (xmax, ymax)
\item Row 4: top-left (xmin, ymax)
}

This forms a closed rectangle suitable for plotting.
This is not a scalar, so cannot be used with \code{measure()}.
}
\examples{
get_bbox(shapes$cat)

# Plot shape with bounding box
p(shapes$cat)
draw_outlines(list(shapes$cat))
draw_outlines(list(get_bbox(shapes$cat)), col = "red")

}
\keyword{internal}
