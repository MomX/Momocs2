% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coo.R
\name{get_transformed}
\alias{get_transformed}
\title{Apply transformation to coordinates}
\usage{
get_transformed(x, ..., .cols = NULL, .ldk_col = NULL)
}
\arguments{
\item{x}{A matrix (nx2), list of matrices, or tibble with coo columns.}

\item{...}{Additional arguments (reserved for future use).}

\item{.cols}{Column name(s) to process when \code{x} is a tibble. If \code{NULL},
automatically detects columns containing coo columns.}

\item{.ldk_col}{Character. Name of landmark column. If \code{NULL}, uses \code{colname_ldk}.}

\item{transform}{A list with elements \code{scale}, \code{rotation}, and \code{translation},
typically from \code{get_transform()}.}
}
\value{
\itemize{
\item If \code{x} is a single matrix: returns the transformed matrix
\item If \code{x} is a list: returns a list of transformed matrices
\item If \code{x} is a tibble: returns the tibble with transformed coo column(s)
}
}
\description{
Apply a transformation (scale, rotation, translation) to shape coordinates.
}
\details{
Applies transformation in order:
\enumerate{
\item Scale (multiply coordinates by scale factor)
\item Rotate (by rotation angle in radians)
\item Translate (shift by translation vector)
}

Landmarks are transformed along with coordinates.
}
\examples{
source <- matrix(c(0,0, 1,0, 1,1, 0,1), ncol=2, byrow=TRUE)
target <- source * 2  # Scaled by 2

# Extract transformation
transform <- get_transform(source, target)

# Apply to another shape
new_shape <- matrix(c(0,0, 2,0, 2,2), ncol=2, byrow=TRUE)
transformed <- get_transformed(new_shape, transform)

}
\seealso{
\code{\link[=get_transform]{get_transform()}}
}
\keyword{internal}
