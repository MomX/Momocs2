% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coo.R
\name{coo_direction_filter}
\alias{coo_direction_filter}
\alias{coo_up}
\alias{coo_down}
\alias{coo_right}
\alias{coo_left}
\title{Filter points by direction from centroid}
\usage{
coo_up(x, ..., .cols = NULL, .ldk_col = NULL)

coo_down(x, ..., .cols = NULL, .ldk_col = NULL)

coo_right(x, ..., .cols = NULL, .ldk_col = NULL)

coo_left(x, ..., .cols = NULL, .ldk_col = NULL)
}
\arguments{
\item{x}{A matrix (nx2), list of matrices, or tibble with coo columns.}

\item{...}{Additional arguments (reserved for future use).}

\item{.cols}{Column name(s) to process when \code{x} is a tibble. If \code{NULL},
automatically detects columns containing coo columns.}

\item{.ldk_col}{Character. Name of landmark column. If \code{NULL}, uses \code{colname_ldk}.}
}
\value{
\itemize{
\item If \code{x} is a single matrix: returns the filtered matrix (open curve)
\item If \code{x} is a list: returns a list of filtered matrices
\item If \code{x} is a tibble: returns the tibble with filtered coo column(s)
}
}
\description{
Keep only points in a specific direction relative to the centroid.
}
\details{
Filters points based on their position relative to the centroid:
\itemize{
\item \strong{\code{coo_up()}}: Keep points with y > centroid_y
\item \strong{\code{coo_down()}}: Keep points with y < centroid_y
\item \strong{\code{coo_right()}}: Keep points with x > centroid_x
\item \strong{\code{coo_left()}}: Keep points with x < centroid_x
}

Returns an open curve. Landmarks are synced.
}
\examples{
# Keep upper half
coo_up(shapes$cat)

# Keep right half
coo_right(bot$coo)

# Keep lower half
coo_down(shapes$cat)

# Keep left half with landmarks
coo_left(hearts)

}
\seealso{
\code{\link[=coo_extract]{coo_extract()}} for extracting specific points
}
\keyword{internal}
