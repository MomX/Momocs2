% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coo.R
\name{coo_slide}
\alias{coo_slide}
\alias{coo_slide_closest}
\alias{coo_slide_angle}
\alias{coo_slide_direction}
\alias{coo_slide_gap}
\title{Slide coordinates along outline}
\usage{
coo_slide(x, ..., .cols = NULL, .ldk_col = NULL)

coo_slide_closest(x, ..., .cols = NULL, .ldk_col = NULL)

coo_slide_angle(x, ..., .cols = NULL, .ldk_col = NULL)

coo_slide_direction(x, ..., .cols = NULL, .ldk_col = NULL)

coo_slide_gap(x, ..., .cols = NULL, .ldk_col = NULL)
}
\arguments{
\item{x}{A matrix (nx2), list of matrices, or tibble with coo columns.}

\item{...}{Additional arguments (reserved for future use).}

\item{.cols}{Column name(s) to process when \code{x} is a tibble. If \code{NULL},
automatically detects columns containing coo objects.}

\item{.ldk_col}{Character. Name of landmark column. If \code{NULL}, uses \code{colname_ldk}.}

\item{id}{Integer. Index of point to become the first point. Default is 1.}

\item{target}{Numeric vector of length 2 (x, y). Find closest point to target.}

\item{theta}{Numeric. Angle in radians. Find point closest to this angle.}

\item{direction}{Character. Direction ("right", "up", "left", "down").}
}
\value{
\itemize{
\item If \code{x} is a single matrix: returns the matrix with reordered points
\item If \code{x} is a list: returns a list of matrices with reordered points
\item If \code{x} is a tibble: returns the tibble with specified coo columns reordered
}
}
\description{
Rotate the order of points along the outline without changing the shape.
}
\details{
\itemize{
\item \code{coo_slide()}: slide to specific index
\item \code{coo_slide_closest()}: slide to point closest to spatial target
\item \code{coo_slide_angle()}: slide to point closest to angle direction
\item \code{coo_slide_direction()}: slide to point in specified direction
\item \code{coo_slide_gap()}: slide to largest gap in perimeter
}

These functions are landmark-aware: landmark indices are automatically
shifted (with wrapping) to match the new point order.
}
\keyword{internal}
