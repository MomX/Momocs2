% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/measure.R
\name{measure_area}
\alias{measure_area}
\title{Measure area of a closed outline}
\usage{
measure_area(x, ..., .cols = NULL, .name = NULL)
}
\arguments{
\item{x}{A matrix (nx2), list of matrices, or tibble with coo columns.}

\item{...}{Additional arguments (reserved for future use).}

\item{.cols}{Column name(s) to process when \code{x} is a tibble. If \code{NULL},
automatically detects columns containing coo objects.}

\item{.name}{Character. Name for the new column when \code{x} is a tibble. If \code{NULL},
uses "colname_area" (e.g., "coo_area").}
}
\value{
\itemize{
\item If \code{x} is a single matrix: returns a numeric scalar
\item If \code{x} is a list: returns a numeric vector of class "meas"
\item If \code{x} is a tibble: returns the tibble with new area column(s) added
}
}
\description{
Calculate the area enclosed by a shape using the shoelace formula.
}
\details{
Uses the shoelace formula (also called surveyor's formula) to compute area.
The outline is automatically treated as closed (first point connects to last).
}
\examples{
measure_area(shapes$cat)
measure_area(shapes)
measure_area(bot)
measure_area(bot, .name = "area")
measure_area(bot, .cols = "coo", .name = "my_area")

}
\seealso{
\code{\link[=measure_perim]{measure_perim()}} for perimeter; \code{\link[=measure_centroid_size]{measure_centroid_size()}} for centroid size
}
\keyword{internal}
