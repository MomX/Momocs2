% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get.R
\name{get_centroid_size_norm}
\alias{get_centroid_size_norm}
\alias{centsize_norm}
\title{Get normalized centroid size}
\usage{
get_centroid_size_norm(x, ..., .cols = NULL)

centsize_norm(x, ..., .cols = NULL)
}
\arguments{
\item{x}{A matrix (nx2), list of matrices, or tibble with coo columns.}

\item{...}{Additional arguments (reserved for future use).}

\item{.cols}{Column name(s) to process when \code{x} is a tibble. If \code{NULL},
automatically detects columns containing coo objects.}
}
\value{
\itemize{
\item If \code{x} is a single matrix: returns a numeric scalar
\item If \code{x} is a list: returns a numeric vector
\item If \code{x} is a tibble: returns a numeric vector extracted from coo column
}
}
\description{
Calculate the normalized centroid size (centroid size divided by perimeter).
}
\details{
For tibbles, this function extracts values without modifying the tibble.
Use within \code{mutate()} to add as a column: \code{mutate(df, cs_norm = get_centroid_size_norm(coo))}
}
\examples{
get_centroid_size_norm(shapes$cat)
get_centroid_size_norm(shapes)

# Extract from tibble
sizes_norm <- get_centroid_size_norm(bot)

\dontrun{
 # Add to tibble when dplyr is loaded
bot \%>\% mutate(cs_norm = get_centroid_size_norm(coo))
}
}
\keyword{internal}
