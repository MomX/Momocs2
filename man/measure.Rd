% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/measure.R
\name{measure}
\alias{measure}
\title{Measure shape properties}
\usage{
measure(x, measures, ..., .cols = NULL, .prefix = NULL)
}
\arguments{
\item{x}{A tibble with coo columns.}

\item{measures}{Character vector of measurement names to compute.
See Details for available measurements.}

\item{...}{Additional arguments passed to measurement functions.}

\item{.cols}{Column name(s) to process. If \code{NULL}, automatically detects
columns containing coo objects.}

\item{.prefix}{Character. Custom prefix for new column names. If \code{NULL},
uses "colname_measurename" (e.g., "coo_area").}
}
\value{
The input tibble with new measurement column(s) added.
}
\description{
Add measurement columns to a tibble by computing scalar shape properties.
}
\details{
Available measurements (all require scalar-returning \verb{get_*} functions):

\strong{Size and perimeter:}
\itemize{
\item \code{"area"} - polygon area (shoelace formula)
\item \code{"perim"} - perimeter length
}

\strong{Centroid size:}
\itemize{
\item \code{"centroid_size"} - centroid size (standard)
\item \code{"centroid_size_norm"} - normalized centroid size
}

\strong{Circularity:}
\itemize{
\item \code{"circularity"} - isoperimetric quotient (4πA/P²)
\item \code{"circularity_norm"} - normalized circularity
\item \code{"circularity_haralick"} - mean(radii)/sd(radii) from centroid
}

\strong{Length and aspect ratio:}
\itemize{
\item \code{"length"} - range along major inertia axis
\item \code{"width"} - range along minor inertia axis
\item \code{"elongation"} - aspect ratio (length/width)
\item \code{"rectangularity"} - area/(length×width)
}

\strong{Convex hull measures:}
\itemize{
\item \code{"convexity"} - perim_hull/perim
\item \code{"solidity"} - area/area_hull
}

\strong{Complex shape descriptors:}
\itemize{
\item \code{"rectilinearity"} - rectangular fit after optimal rotation
\item \code{"calliper"} - maximum distance between points
}

To see all available measurements, use \code{available_measures()}.
}
\examples{
# Single measurement
bot \%>\% measure("area")

# Multiple measurements
bot \%>\% measure(c("area", "perim", "centroid_size"))

# Custom prefix
bot \%>\% measure(c("area", "perim"), .prefix = "shape")

# On specific column
bot \%>\% measure("area", .cols = coo)

}
\seealso{
\code{\link[=available_measures]{available_measures()}} for list of measurements; \verb{get_*} functions for extraction
}
\keyword{internal}
