% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get.R
\name{get_transform}
\alias{get_transform}
\title{Extract transformation between two configurations}
\usage{
get_transform(source, target)
}
\arguments{
\item{source}{Matrix (nx2). Source configuration.}

\item{target}{Matrix (nx2). Target configuration.}
}
\value{
A list with three elements:
\itemize{
\item \code{scale}: Numeric. Scale factor.
\item \code{rotation}: Numeric. Rotation angle in radians.
\item \code{translation}: Numeric vector of length 2. Translation (dx, dy).
}
}
\description{
Calculate the scale, rotation, and translation needed to transform one
configuration into another.
}
\details{
Calculates transformation based on the furthest pair of points in the source
configuration. Finds the corresponding pair in the target and extracts:
\itemize{
\item Scale: ratio of distances between furthest points
\item Rotation: angle difference between the two vectors
\item Translation: shift after scaling and rotation
}

Both configurations must have the same number of points.
}
\examples{
source <- matrix(c(0,0, 1,0, 1,1, 0,1), ncol=2, byrow=TRUE)
target <- source * 2  # Scaled by 2
transform <- get_transform(source, target)
transform$scale  # Should be ~2

}
\seealso{
\code{\link[=get_transformed]{get_transformed()}}
}
\keyword{internal}
