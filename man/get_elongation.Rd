% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get.R
\name{get_elongation}
\alias{get_elongation}
\title{Get elongation}
\usage{
get_elongation(x, ..., .cols = NULL)
}
\arguments{
\item{x}{A matrix (nx2), list of matrices, or tibble with coo columns.}

\item{...}{Additional arguments (reserved for future use).}

\item{.cols}{Column name(s) to process when \code{x} is a tibble. If \code{NULL},
automatically detects columns containing coo objects.}
}
\value{
\itemize{
\item If \code{x} is a single matrix: returns a numeric scalar
\item If \code{x} is a list: returns a numeric vector
\item If \code{x} is a tibble: returns a numeric vector extracted from coo column
}
}
\description{
Calculate elongation (aspect ratio) as length / width.
}
\details{
Elongation is the aspect ratio based on inertia axes: length / width.
Value of 1 indicates equal length and width. Higher values indicate more elongated shapes.

This is equivalent to eccentricity based on bounding box dimensions.
}
\examples{
get_elongation(shapes$cat)

# Use in measure()
bot \%>\% measure("elongation")

}
\seealso{
\code{\link[=get_length]{get_length()}}, \code{\link[=get_width]{get_width()}}, \code{\link[=get_rectangularity]{get_rectangularity()}}
}
\keyword{internal}
