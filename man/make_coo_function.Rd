% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coo.R
\name{make_coo_function}
\alias{make_coo_function}
\title{Create a coo function with automatic dispatch}
\usage{
make_coo_function(impl_fn, fn_name = NULL, sync_ldk = FALSE)
}
\arguments{
\item{impl_fn}{Function. The implementation function that operates on a single
matrix (nx2). Should accept \code{x} as first argument and \code{...} for additional args.}

\item{fn_name}{Character. Optional name for debugging (not used functionally).}

\item{sync_ldk}{Logical. If TRUE, function is landmark-aware and will sync
landmark columns automatically. Default is FALSE.}
}
\value{
A function that dispatches based on input type.
}
\description{
Higher-order function factory that creates coo_ functions with automatic
dispatch to handle single matrices, lists of matrices, and tibbles.
}
\details{
The returned function automatically:
\itemize{
\item Applies impl_fn to single matrices and returns the result
\item Applies impl_fn to each element of a list and returns a list
\item Applies impl_fn to specified columns of a tibble and returns the modified tibble
}

When \code{sync_ldk = TRUE}, the dispatcher automatically handles landmark columns:
\itemize{
\item Looks for a column named \code{colname_ldk} (e.g., \code{coo_ldk} for \code{coo} column)
\item Or uses \code{.ldk_col} argument if provided
\item Passes landmarks to implementation function
\item Updates landmarks based on coordinate changes
}

Additional arguments are passed through via \code{...}
}
\keyword{internal}
