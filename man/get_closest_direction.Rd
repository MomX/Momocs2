% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get.R
\name{get_closest_direction}
\alias{get_closest_direction}
\title{Get point closest to a given direction}
\usage{
get_closest_direction(x, ..., .cols = NULL)
}
\arguments{
\item{x}{A matrix (nx2), list of matrices, or tibble with coo columns.}

\item{...}{Additional arguments (reserved for future use).}

\item{.cols}{Column name(s) to process when \code{x} is a tibble. If \code{NULL},
automatically detects columns containing coo objects.}

\item{direction}{Character. One of "right", "up", "left", or "down".}
}
\value{
\itemize{
\item If \code{x} is a single matrix: returns an integer (point index)
\item If \code{x} is a list: returns an integer vector
\item If \code{x} is a tibble: returns an integer vector extracted from coo column
}
}
\description{
Find the index of the point closest to a specified direction from the centroid.
}
\details{
Centers the shape, calculates the angle from centroid to each point,
and returns the index of the point whose angle is closest to the specified direction:
\itemize{
\item "right" = 0 radians (East)
\item "up" = pi/2 radians (North)
\item "left" = pi radians (West)
\item "down" = -pi/2 radians (South)
}
}
\examples{
# Find point closest to East
get_closest_direction(shapes$cat, direction = "right")

# Find point closest to North
get_closest_direction(shapes$cat, direction = "up")

}
\seealso{
\code{\link[=get_closest]{get_closest()}}, \code{\link[=get_closest_angle]{get_closest_angle()}}
}
\keyword{internal}
