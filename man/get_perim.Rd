% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get.R
\name{get_perim}
\alias{get_perim}
\alias{get_perim_along}
\alias{get_perim_cum}
\title{Get perimeter measurements}
\usage{
get_perim(x, ..., .cols = NULL)

get_perim_along(x, ..., .cols = NULL)

get_perim_cum(x, ..., .cols = NULL)
}
\arguments{
\item{x}{A matrix (nx2), list of matrices, or tibble with coo columns.}

\item{...}{Additional arguments (reserved for future use).}

\item{.cols}{Column name(s) to process when \code{x} is a tibble. If \code{NULL},
automatically detects columns containing coo objects.}
}
\value{
\itemize{
\item \code{get_perim()}: numeric scalar (matrix) or vector (list/tibble)
\item \code{get_perim_along()}: numeric vector (matrix) or list of vectors (list/tibble)
\item \code{get_perim_cum()}: numeric vector (matrix) or list of vectors (list/tibble)
}
}
\description{
Calculate perimeter-related measurements of a shape.
}
\details{
\itemize{
\item \code{get_perim()}: total perimeter
\item \code{get_perim_along()}: length of each segment
\item \code{get_perim_cum()}: cumulative perimeter from start
}

For tibbles, these functions extract values without modifying the tibble.
}
\examples{
get_perim(shapes$cat)
get_perim_along(shapes$cat)
get_perim_cum(shapes$cat)

# Extract from tibble
perims <- get_perim(bot)

# Add to tibble
bot$perim <- get_perim(bot)

}
\keyword{internal}
