% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coo.R
\name{coo_rotate}
\alias{coo_rotate}
\title{Rotate coordinates}
\usage{
coo_rotate(x, ..., .cols = NULL, .ldk_col = NULL)
}
\arguments{
\item{x}{A matrix (nx2), list of matrices, or tibble with coo columns.}

\item{...}{Additional arguments (reserved for future use).}

\item{.cols}{Column name(s) to process when \code{x} is a tibble. If \code{NULL},
automatically detects columns containing coo objects.}

\item{theta}{Numeric. Rotation angle in radians. Default is 0.}
}
\value{
\itemize{
\item If \code{x} is a single matrix: returns the rotated matrix
\item If \code{x} is a list: returns a list of rotated matrices
\item If \code{x} is a tibble: returns the tibble with specified coo columns rotated
}
}
\description{
Rotate a shape by a specified angle around the origin.
}
\examples{
coo_rotate(shapes$cat, theta = pi / 4)
coo_rotate(shapes, theta = pi / 6)
coo_rotate(bot, theta = pi / 8)

}
\seealso{
\code{\link[=coo_rotate_around]{coo_rotate_around()}} for rotation around a specific point; \code{\link[=coo_slide_angle]{coo_slide_angle()}} for rotating point order by angle
}
\keyword{internal}
