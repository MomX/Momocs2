% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/morpho_efourier.R
\name{efourier_i}
\alias{efourier_i}
\title{Inverse elliptic Fourier transform}
\usage{
efourier_i(x, nb_h = NULL, nb_pts = 120, ..., .cols = NULL, .name = NULL)
}
\arguments{
\item{x}{A numeric vector, list (from raw efourier), list of vectors, or
tibble with coe columns.}

\item{nb_h}{Integer. Number of harmonics to use for reconstruction. If \code{NULL},
uses all available harmonics.}

\item{nb_pts}{Integer. Number of points to reconstruct. Default is 120.}

\item{...}{Additional arguments (reserved for future use).}

\item{.cols}{Column name(s) to process when \code{x} is a tibble. If \code{NULL},
automatically detects columns containing coe objects.}

\item{.name}{Character. Name for the output coo column when \code{x} is a tibble.
If \code{NULL}, uses the pattern \code{"colname_i"} (e.g., \code{"coe_i"}).}
}
\value{
\itemize{
\item If \code{x} is a vector or raw list: returns a matrix (nb_pts x 2) with class \code{"xy"}
\item If \code{x} is a list of vectors: returns a list of matrices
\item If \code{x} is a tibble: returns the tibble with a new coo column added
}
}
\description{
Reconstruct outline coordinates from elliptic Fourier coefficients.
}
\details{
Performs inverse elliptic Fourier transform to reconstruct outline coordinates
from coefficients. This is useful for:
\itemize{
\item Visualizing shapes at different harmonic levels
\item Reconstructing shapes from PCA or other statistical analyses
\item Filtering out high-frequency noise by using fewer harmonics
}

If \code{nb_h} is less than the total number of harmonics in the coefficients,
only the first \code{nb_h} harmonics are used (low-pass filtering).
}
\examples{
# Single outline
coo <- matrix(runif(100), ncol = 2)
coefs <- efourier(coo, nb_h = 6)

# Reconstruct with all harmonics
coo_reconstructed <- efourier_i(coefs)

# Reconstruct with fewer harmonics (smoothing)
coo_smooth <- efourier_i(coefs, nb_h = 3)

# From raw output
coefs_raw <- efourier(coo, raw = TRUE)
efourier_i(coefs_raw)

\dontrun{
# Tibble workflow
library(dplyr)
bot \%>\%
  efourier() \%>\%
  efourier_i()

# Custom column names
bot \%>\%
  efourier(.name = "eft") \%>\%
  efourier_i(.cols = eft, .name = "coo_reconstructed")
}

}
\seealso{
\code{\link[=efourier]{efourier()}} for forward transform
}
