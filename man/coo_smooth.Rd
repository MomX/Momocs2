% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coo.R
\name{coo_smooth}
\alias{coo_smooth}
\alias{coo_smooth_fixed}
\title{Smooth coordinates}
\usage{
coo_smooth(x, ..., .cols = NULL, .ldk_col = NULL)

coo_smooth_fixed(x, ..., .cols = NULL, .ldk_col = NULL)
}
\arguments{
\item{x}{A matrix (nx2), list of matrices, or tibble with coo columns.}

\item{...}{Additional arguments (reserved for future use).}

\item{.cols}{Column name(s) to process when \code{x} is a tibble. If \code{NULL},
automatically detects columns containing coo objects.}

\item{n}{Integer. Number of smoothing iterations. Default is 0 (no smoothing).}
}
\value{
\itemize{
\item If \code{x} is a single matrix: returns the smoothed matrix
\item If \code{x} is a list: returns a list of smoothed matrices
\item If \code{x} is a tibble: returns the tibble with specified coo columns smoothed
}
}
\description{
Smooth a shape using local averaging.
}
\details{
\itemize{
\item \code{coo_smooth()}: smooth all points
\item \code{coo_smooth_fixed()}: smooth while keeping first and last points fixed
}
}
\examples{
# Smooth with 3 iterations
coo_smooth(shapes$cat, n = 3)

# Smooth list
coo_smooth(bot$coo, n = 5)

# Smooth with fixed endpoints
coo_smooth_fixed(shapes$cat, n = 3)

}
\seealso{
\code{\link[=coo_sample]{coo_sample()}} for resampling
}
\keyword{internal}
