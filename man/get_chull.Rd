% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get.R
\name{get_chull}
\alias{get_chull}
\alias{get_chull_id}
\title{Get convex hull}
\usage{
get_chull(x, ..., .cols = NULL)

get_chull_id(x, ..., .cols = NULL)
}
\arguments{
\item{x}{A matrix (nx2), list of matrices, or tibble with coo columns.}

\item{...}{Additional arguments (reserved for future use).}

\item{.cols}{Column name(s) to process when \code{x} is a tibble. If \code{NULL},
automatically detects columns containing coo objects.}
}
\value{
\itemize{
\item \code{get_chull()}: convex hull coordinates as a matrix
\item \code{get_chull_id()}: indices of convex hull points as an integer vector
}
}
\description{
Calculate the convex hull coordinates or indices.
}
\details{
Uses \code{grDevices::chull()} to compute the convex hull.
The convex hull is the smallest convex polygon containing all points.

Note: Returns non-scalar values, so cannot be used with \code{measure()}.
}
\examples{
get_chull(shapes$cat)
get_chull_id(shapes$cat)

}
\seealso{
\code{\link[=get_convexity]{get_convexity()}}, \code{\link[=get_solidity]{get_solidity()}}
}
\keyword{internal}
