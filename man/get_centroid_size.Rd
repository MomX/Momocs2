% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get.R
\name{get_centroid_size}
\alias{get_centroid_size}
\alias{centsize}
\title{Get centroid size}
\usage{
get_centroid_size(x, ..., .cols = NULL)

centsize(x, ..., .cols = NULL)
}
\arguments{
\item{x}{A matrix (nx2), list of matrices, or tibble with coo columns.}

\item{...}{Additional arguments (reserved for future use).}

\item{.cols}{Column name(s) to process when \code{x} is a tibble. If \code{NULL},
automatically detects columns containing coo objects.}
}
\value{
\itemize{
\item If \code{x} is a single matrix: returns a numeric scalar
\item If \code{x} is a list: returns a numeric vector
\item If \code{x} is a tibble: returns a numeric vector extracted from coo column
}
}
\description{
Calculate the centroid size (CS): the square root of the sum of squared
distances from each point to the centroid.
}
\details{
Centroid size is a common size measure in geometric morphometrics.
It is scale-independent and used for allometric correction.

For tibbles, this function extracts values without modifying the tibble.
Use within \code{mutate()} to add as a column: \code{mutate(df, cs = get_centroid_size(coo))}
}
\examples{
get_centroid_size(shapes$cat)
get_centroid_size(shapes)

# Extract from tibble
sizes <- get_centroid_size(bot)

\dontrun{
 # Add to tibble when dplyr is loaded
bot \%>\% mutate(centsize = get_centroid_size(coo))
}

}
\keyword{internal}
