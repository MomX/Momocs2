% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/morpho_npoly.R
\name{npoly_i}
\alias{npoly_i}
\title{Inverse natural polynomial transform}
\usage{
npoly_i(x, nb_pts = 120, ..., .cols = NULL, .name = "_i")
}
\arguments{
\item{x}{A numeric vector, list (from raw npoly), list of vectors, or
tibble with coe columns.}

\item{nb_pts}{Integer. Number of points to reconstruct. Default is 120.}

\item{...}{Additional arguments (reserved for future use).}

\item{.cols}{Column name(s) to process when \code{x} is a tibble. If \code{NULL},
processes all coe columns automatically.}

\item{.name}{Character. Name suffix for output curve columns when \code{x} is a tibble.
Default is \code{"_i"}. Output columns will be named \code{originalname_i}.}
}
\value{
\itemize{
\item If \code{x} is a vector or raw list: returns a matrix (nb_pts x 2) with class \code{"xy"}
\item If \code{x} is a list of vectors: returns a list of matrices
\item If \code{x} is a tibble: returns the tibble with new cur column(s) added
}
}
\description{
Reconstruct curve coordinates from natural polynomial coefficients.
}
\details{
Performs inverse natural polynomial transform to reconstruct curve coordinates
from coefficients. The curve is reconstructed between baseline points at
(-0.5, 0) and (0.5, 0), then re-registered to original baseline.
}
\examples{
# Single curve
data(olea)
cur <- olea$VD[[1]]
coefs <- npoly(cur, degree = 6)

# Reconstruct
cur_reconstructed <- npoly_i(coefs)

# From raw output
coefs_raw <- npoly(cur, raw = TRUE)
npoly_i(coefs_raw)

\dontrun{
# Tibble workflow
library(dplyr)
olea \%>\%
  npoly(.cols = VD) \%>\%
  npoly_i()  # Creates VD_coe_i

# Custom column names
olea \%>\%
  npoly(.cols = VD) \%>\%
  npoly_i(.cols = VD_coe, .name = "VD_reconstructed")
}

}
\seealso{
\code{\link[=npoly]{npoly()}} for forward transform
}
