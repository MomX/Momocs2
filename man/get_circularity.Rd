% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get.R
\name{get_circularity}
\alias{get_circularity}
\alias{get_circularity_norm}
\alias{get_circularity_haralick}
\title{Get circularity measures}
\usage{
get_circularity(x, ..., .cols = NULL)

get_circularity_norm(x, ..., .cols = NULL)

get_circularity_haralick(x, ..., .cols = NULL)
}
\arguments{
\item{x}{A matrix (nx2), list of matrices, or tibble with coo columns.}

\item{...}{Additional arguments (reserved for future use).}

\item{.cols}{Column name(s) to process when \code{x} is a tibble. If \code{NULL},
automatically detects columns containing coo objects.}
}
\value{
\itemize{
\item If \code{x} is a single matrix: returns a numeric scalar
\item If \code{x} is a list: returns a numeric vector
\item If \code{x} is a tibble: returns a numeric vector extracted from coo column
}
}
\description{
Calculate circularity (isoperimetric quotient) and related shape descriptors.
}
\details{
\strong{Circularity measures:}
\itemize{
\item \code{get_circularity()}: Isoperimetric quotient = 4π × Area / Perimeter².
Value of 1 for a perfect circle, < 1 for other shapes.
\item \code{get_circularity_norm()}: Normalized version = Area / (Perimeter²/(4π)).
Alternative formulation, same interpretation.
\item \code{get_circularity_haralick()}: Mean(radii) / SD(radii) from centroid.
Higher values = more circular. Sensitive to irregularities.
}

All measures equal 1 for perfect circles and decrease for other shapes.
}
\examples{
get_circularity(shapes$cat)
get_circularity_norm(shapes$cat)
get_circularity_haralick(shapes$cat)

# Use in measure()
bot \%>\% measure("circularity")
bot \%>\% measure(c("circularity", "circularity_haralick"))

}
\seealso{
\code{\link[=measure]{measure()}} for adding measurement columns
}
\keyword{internal}
