% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coo.R
\name{coo_reverse}
\alias{coo_reverse}
\title{Reverse point order}
\usage{
coo_reverse(x, ..., .cols = NULL, .ldk_col = NULL)
}
\arguments{
\item{x}{A matrix (nx2), list of matrices, or tibble with coo columns.}

\item{...}{Additional arguments (reserved for future use).}

\item{.cols}{Column name(s) to process when \code{x} is a tibble. If \code{NULL},
automatically detects columns containing coo objects.}

\item{.ldk_col}{Character. Name of landmark column. If \code{NULL}, uses \code{colname_ldk}.}
}
\value{
\itemize{
\item If \code{x} is a single matrix: returns the matrix with reversed point order
\item If \code{x} is a list: returns a list of matrices with reversed point order
\item If \code{x} is a tibble: returns the tibble with specified coo columns reversed
}
}
\description{
Reverse the order of points along an outline.
}
\details{
This function is landmark-aware: landmark indices are automatically
reversed to match the new point order using the formula \code{n - ldk + 1}.
}
\examples{
# Reverse single shape
coo_reverse(shapes$cat)

# Reverse list of shapes
coo_reverse(bot$coo)

# Reverse with landmarks
coo_reverse(hearts)

}
\seealso{
\code{\link[=coo_direction_positive]{coo_direction_positive()}} for ensuring counter-clockwise direction; \code{\link[=coo_slide_id]{coo_slide_id()}} for reordering
}
\keyword{internal}
