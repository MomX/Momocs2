% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coo.R
\name{coo_scale}
\alias{coo_scale}
\title{Scale to unit centroid size}
\usage{
coo_scale(x, ..., .cols = NULL, .ldk_col = NULL)
}
\arguments{
\item{x}{A matrix (nx2), list of matrices, or tibble with coo columns.}

\item{...}{Additional arguments (reserved for future use).}

\item{.cols}{Column name(s) to process when \code{x} is a tibble. If \code{NULL},
automatically detects columns containing coo objects.}

\item{.ldk_col}{Character. Name of landmark column. If \code{NULL}, uses \code{colname_ldk}.}
}
\value{
\itemize{
\item If \code{x} is a single matrix: returns the scaled matrix
\item If \code{x} is a list: returns a list of scaled matrices
\item If \code{x} is a tibble: returns the tibble with scaled coo column(s)
}
}
\description{
Scale shape coordinates to unit centroid size (without centering).
}
\details{
Scales coordinates to unit centroid size: divides all coordinates by centroid size.
Equivalent to \code{scale(x, center = FALSE, scale = TRUE)} in base R.

Does NOT center the shape first. Use \code{coo_center()} before \code{coo_scale()} if needed.

For rescaling by a custom factor, use \code{coo_rescale()}.

Landmarks are synced.
}
\examples{
# Scale to unit centroid size
coo_scale(shapes$cat)

# Center then scale
shapes$cat \%>\% coo_center() \%>\% coo_scale()

}
\seealso{
\code{\link[=coo_rescale]{coo_rescale()}} for custom scaling; \code{\link[=coo_center]{coo_center()}} for centering
}
\keyword{internal}
