% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/imports.R
\name{pipes}
\alias{pipes}
\alias{\%>\%}
\alias{\%T>\%}
\alias{\%$\%}
\title{Pipe operators}
\arguments{
\item{lhs}{A value or the magrittr placeholder.}

\item{rhs}{A function call using the magrittr semantics.}
}
\description{
Momocs2 uses magrittr's pipe operators for clear, readable workflows.
}
\details{
\strong{Forward pipe (\verb{\%>\%})}: Passes result forward to next function

\if{html}{\out{<div class="sourceCode r">}}\preformatted{bot \%>\%
  coo_center() \%>\%
  coo_scale() \%>\%
  measure(c("area", "circularity"))
}\if{html}{\out{</div>}}

\strong{Tee pipe (\verb{\%T>\%})}: Passes input forward unchanged (useful for side effects like plotting)

\if{html}{\out{<div class="sourceCode r">}}\preformatted{bot \%>\%
  coo_center() \%T>\%
  pile() \%>\%           # Plot without breaking the chain
  coo_scale()
}\if{html}{\out{</div>}}

\strong{Exposition pipe (\verb{\%$\%})}: Exposes column names for direct use

\if{html}{\out{<div class="sourceCode r">}}\preformatted{bot \%>\%
  measure(c("area", "perim")) \%$\%
  plot(coo_area, coo_perim)  # Use columns directly without bot$
}\if{html}{\out{</div>}}
}
\examples{
# Forward pipe - standard workflow.
# Almost exactly equivalent to (now native) |>
bot \%>\%
  coo_center() \%>\%
  coo_scale() \%>\%
  measure("area")

# Tee pipe - plot without breaking chain
bot \%>\%
  coo_center() \%T>\%
  pile() \%>\%           # Plot without breaking the chain
  coo_scale()

# Exposition pipe - access columns directly
bot \%>\%
  measure(c("area", "perim")) \%$\%
  plot(coo_area, coo_perim)
}
\seealso{
\code{magrittr} vignettes for detailed documentation.
}
\keyword{internal}
