% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/measure.R
\name{measure_centroid_size_norm}
\alias{measure_centroid_size_norm}
\title{Measure normalized centroid size}
\usage{
measure_centroid_size_norm(x, ..., .cols = NULL, .name = NULL)
}
\arguments{
\item{x}{A matrix (nx2), list of matrices, or tibble with coo columns.}

\item{...}{Additional arguments (reserved for future use).}

\item{.cols}{Column name(s) to process when \code{x} is a tibble. If \code{NULL},
automatically detects columns containing coo objects.}

\item{.name}{Character. Name for the new column when \code{x} is a tibble. If \code{NULL},
uses "colname_centroid_size_norm" (e.g., "coo_centroid_size_norm").}
}
\value{
\itemize{
\item If \code{x} is a single matrix: returns a numeric scalar
\item If \code{x} is a list: returns a numeric vector of class "meas"
\item If \code{x} is a tibble: returns the tibble with new normalized centroid size column(s) added
}
}
\description{
Calculate the normalized centroid size (centroid size divided by perimeter).
}
\details{
Normalized centroid size provides a scale-independent measure that accounts
for both size (centroid size) and shape complexity (perimeter).
}
\examples{
measure_centroid_size_norm(shapes$cat)
measure_centroid_size_norm(shapes)
measure_centroid_size_norm(bot)
measure_centroid_size_norm(bot, .name = "CS_norm")

}
\seealso{
\code{\link[=measure_centroid_size]{measure_centroid_size()}} for centroid size; \code{\link[=get_centroid_size_norm]{get_centroid_size_norm()}} for the getter function
}
\keyword{internal}
