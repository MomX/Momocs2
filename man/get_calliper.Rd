% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get.R
\name{get_calliper}
\alias{get_calliper}
\alias{get_calliper_ids}
\title{Get calliper}
\usage{
get_calliper(x, ..., .cols = NULL)

get_calliper_ids(x, ..., .cols = NULL)
}
\arguments{
\item{x}{A matrix (nx2), list of matrices, or tibble with coo columns.}

\item{...}{Additional arguments (reserved for future use).}

\item{.cols}{Column name(s) to process when \code{x} is a tibble. If \code{NULL},
automatically detects columns containing coo objects.}
}
\value{
\itemize{
\item \code{get_calliper()}: maximum distance as a numeric scalar
\item \code{get_calliper_ids()}: indices of the two furthest points as a vector c(i, j)
}
}
\description{
Calculate the maximum distance between any two points (calliper/Feret diameter).
}
\details{
Calculates the maximum distance between any two points on the outline.
Also known as Feret diameter or maximum calliper distance.

Uses brute force O(n²) algorithm. May be slow for outlines with many points.

Note: \code{get_calliper_ids()} returns a vector, not a scalar, so cannot be used with \code{measure()}.
}
\examples{
get_calliper(shapes$cat)
get_calliper_ids(shapes$cat)

# Use in measure()
bot \%>\% measure("calliper")

}
\keyword{internal}
