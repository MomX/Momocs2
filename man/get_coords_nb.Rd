% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get.R
\name{get_coords_nb}
\alias{get_coords_nb}
\title{Get number of coordinates}
\usage{
get_coords_nb(x, ..., .cols = NULL)
}
\arguments{
\item{x}{A matrix (nx2), list of matrices, or tibble with coo columns.}

\item{...}{Additional arguments (reserved for future use).}

\item{.cols}{Column name(s) to process when \code{x} is a tibble. If \code{NULL},
automatically detects columns containing coo objects.}
}
\value{
\itemize{
\item If \code{x} is a single matrix: returns an integer
\item If \code{x} is a list: returns a numeric vector
\item If \code{x} is a tibble: returns a numeric vector extracted from coo column
}
}
\description{
Count the number of coordinate points in a shape.
}
\details{
Simply returns the number of rows (coordinate points) in the shape.
Can be used with \code{measure()}.
}
\examples{
get_coords_nb(shapes$cat)
get_coords_nb(shapes)

# Use in measure()
bot \%>\% measure("coords_nb")

}
\keyword{internal}
