% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coo.R
\name{coo_sample_regular_radius}
\alias{coo_sample_regular_radius}
\title{Sample at regular angles from centroid}
\usage{
coo_sample_regular_radius(x, ..., .cols = NULL, .ldk_col = NULL)
}
\arguments{
\item{x}{A matrix (nx2), list of matrices, or tibble with coo columns.}

\item{...}{Additional arguments (reserved for future use).}

\item{.cols}{Column name(s) to process when \code{x} is a tibble. If \code{NULL},
automatically detects columns containing coo columns.}

\item{.ldk_col}{Character. Name of landmark column. If \code{NULL}, uses \code{colname_ldk}.}

\item{n}{Integer. Number of points to sample.}
}
\value{
\itemize{
\item If \code{x} is a single matrix: returns the sampled matrix
\item If \code{x} is a list: returns a list of sampled matrices
\item If \code{x} is a tibble: returns the tibble with sampled coo column(s)
}
}
\description{
Sample points at regular angular intervals from the centroid (radial sampling).
}
\details{
Samples points at regular angular intervals from the centroid, like spokes
on a wheel. For each target angle, finds the outline point closest to that
direction.

Landmarks are NOT preserved - this resampling method fundamentally changes
the point structure.
}
\examples{
coo_sample_regular_radius(shapes$cat, n = 64)

}
\seealso{
\code{\link[=coo_sample]{coo_sample()}}
}
\keyword{internal}
