% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get.R
\name{get_length}
\alias{get_length}
\title{Get length}
\usage{
get_length(x, ..., .cols = NULL)
}
\arguments{
\item{x}{A matrix (nx2), list of matrices, or tibble with coo columns.}

\item{...}{Additional arguments (reserved for future use).}

\item{.cols}{Column name(s) to process when \code{x} is a tibble. If \code{NULL},
automatically detects columns containing coo objects.}
}
\value{
\itemize{
\item If \code{x} is a single matrix: returns a numeric scalar
\item If \code{x} is a list: returns a numeric vector
\item If \code{x} is a tibble: returns a numeric vector extracted from coo column
}
}
\description{
Calculate length along the major inertia axis.
}
\details{
Length is the range along the major inertia axis (largest variance direction).
}
\examples{
get_length(shapes$cat)

# Use in measure()
bot \%>\% measure("length")

}
\seealso{
\code{\link[=get_width]{get_width()}}, \code{\link[=get_lw]{get_lw()}}, \code{\link[=get_elongation]{get_elongation()}}
}
\keyword{internal}
