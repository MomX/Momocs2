% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coo.R
\name{coo_untilt}
\alias{coo_untilt}
\alias{coo_untilt_x}
\alias{coo_untilt_y}
\title{Untilt to axis}
\usage{
coo_untilt_x(x, ..., .cols = NULL, .ldk_col = NULL)

coo_untilt_y(x, ..., .cols = NULL, .ldk_col = NULL)
}
\arguments{
\item{x}{A matrix (nx2), list of matrices, or tibble with coo columns.}

\item{...}{Additional arguments (reserved for future use).}

\item{.cols}{Column name(s) to process when \code{x} is a tibble. If \code{NULL},
automatically detects columns containing coo columns.}
}
\value{
\itemize{
\item If \code{x} is a single matrix: returns the rotated matrix
\item If \code{x} is a list: returns a list of rotated matrices
\item If \code{x} is a tibble: returns the tibble with rotated coo column(s)
}
}
\description{
Rotate shape so the first point is exactly on an axis.
}
\details{
\strong{\code{coo_untilt_x()}}: Rotates around centroid so first point is on x-axis (y=0).

\strong{\code{coo_untilt_y()}}: Rotates around centroid so first point is on y-axis (x=0),
choosing the nearest y-axis (positive or negative).

Works best when shapes are centered.
}
\examples{
# Untilt to x-axis
coo_untilt_x(shapes$cat)

# Untilt to y-axis
coo_untilt_y(bot$coo)

# Works better after centering
shapes$cat \%>\% coo_center() \%>\% coo_untilt_x()

}
\seealso{
\code{\link[=coo_rotate]{coo_rotate()}} for arbitrary rotation; \code{\link[=coo_align]{coo_align()}} for principal axis alignment
}
\keyword{internal}
